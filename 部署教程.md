# 免费发布教程：把「食光重构」挂到 GitHub Pages

> 全程免费，约 10-15 分钟。发布后你会得到一个链接，格式为：
> `https://你的用户名.github.io/siguang-chonggou/`
> 任何人都能打开。这个链接含 `github.io` 字样（免费托管的代价），如果你想要完全不含平台名的链接，需要自己买一个域名（约 ¥20-50/年）绑定，方法见文末。

---

## 第一步：注册 GitHub 账号（已有则跳过）

1. 打开 https://github.com
2. 点右上角 **Sign up**，用邮箱注册
3. 验证邮箱后登录
4. 记下你的 **用户名**（登录后点右上角头像能看到，例如 `zhangsan`）

---

## 第二步：新建仓库

1. 登录后，点右上角 **+** 号 → **New repository**
2. 仓库名（Repository name）填：`siguang-chonggou`（或任意英文名）
3. 选 **Public**（公开，否则别人打不开）
4. 其余默认，点 **Create repository**（创建仓库）

---

## 第三步：上传文件

创建完仓库后，进入仓库主页，点 **uploading an existing file**（上传已有文件）：

1. 把本文件夹里的 `index.html` 拖进上传区域
2. 等它上传完成
3. 下方 **Commit changes** 直接点绿色按钮提交

> 也可以整个文件夹拖进去，`README.md`、`部署教程.md` 可传可不传，`index.html` 是必需的。

---

## 第四步：开启 GitHub Pages

1. 在仓库主页，点顶部 **Settings**（设置）
2. 左侧菜单点 **Pages**
3. 在 **Build and deployment** 下：
   - Source 选 **Deploy from a branch**
   - Branch 选 **main**，目录选 **/ (root)**，点 **Save**
4. 等 1-2 分钟，页面顶部会出现一行绿色提示，显示你的网址：
   `https://你的用户名.github.io/siguang-chonggou/`

---

## 第五步：打开验证

1. 点开那个网址，确认能正常显示「食光重构」页面
2. 试试填问卷、点"生成我的专属食谱"，看结果是否正常
3. 把这个链接发给别人即可

---

## 常见问题

**Q：打开 404？**
A：通常是没等 Pages 生效，或文件名不是 `index.html`。确认文件名严格是 `index.html`（全小写），再等 1-2 分钟刷新。

**Q：想更新内容？**
A：改完本地的 `index.html`，回到仓库 → **uploading an existing file** → 重新拖入同名文件 → 提交即可，链接不变。

**Q：想彻底去掉 github.io 字样？**
A：需要买一个域名（阿里云/腾讯云约 ¥20-50/年），然后：
1. 在域名服务商把域名解析（CNAME）到 `你的用户名.github.io`
2. 在仓库 Settings → Pages → **Custom domain** 填入你的域名并保存
3. 在仓库里放一个名为 `CNAME` 的文件，内容是你的域名
完成后，访问你的域名即可，链接完全由你决定，无任何平台痕迹。

---

## 补充：更省事的发布方式（推荐给不想折腾的人）

如果你不想注册 GitHub，也可以直接把 `index.html` 这个文件通过微信/邮件发给别人，对方用手机或电脑浏览器打开即可使用——它是个完全自包含的单文件，双击就开，效果和链接一样，只是需要"传文件"而不是"点链接"。
